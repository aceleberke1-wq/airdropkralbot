services:
  - type: web
    name: airdropkral-admin
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:all
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: LOOP_V2_ENABLED
        value: "1"
      - key: BOT_DRY_RUN
        value: "0"
      - key: ADMIN_API_PORT
        value: "10000"
      - key: PORT
        value: "10000"
      - key: DATABASE_SSL
        value: "1"
